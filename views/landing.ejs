<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .btn-black {
            background-color: black;
            border-color: black;
            color: white;
        }
        .btn-black:hover {
            background-color: #333;
            border-color: #333;
            color: white;
        }
        .btn-outline-black {
            color: black;
            border-color: black;
        }
        .btn-outline-black:hover {
            background-color: black;
            color: white;
        }
        .navbar-black {
            background-color: black !important;
        }
    </style>
    <% if (authenticated) { %>
        <title>Dashboard</title>
    <% } else { %>
        <title>Landing Page</title>
    <% } %>
</head>
<body class="bg-light">
    <nav class="navbar navbar-expand-lg navbar-dark navbar-black">
        <div class="container">
            <a class="navbar-brand" href="/">Dawgg</a>
        </div>
    </nav>

    <div class="container mt-5">
        <% if (authenticated) { %>
            <div class="card shadow-sm">
                <div class="card-body">
                    <h1 class="card-title">Dashboard</h1>
                    <h2 class="h4 mb-4">Hello, <%= name %></h2>
                    <div class="d-grid gap-2 d-md-block">
                        <a href="/members" class="btn btn-black me-2">Members Area</a>
                        <a href="/logout" class="btn btn-outline-black">Logout</a>
                    </div>
                </div>
            </div>
        <% } else { %>
            <div class="card shadow-sm">
                <div class="card-body text-center">
                    <h1 class="card-title mb-4">Welcome to Our App</h1>
                    <p class="lead mb-4">Join our community today!</p>
                    <div class="d-grid gap-2 d-md-block">
                        <a href="/signup" class="btn btn-black me-2">Sign Up</a>
                        <a href="/login" class="btn btn-outline-black">Login</a>
                    </div>
                </div>
            </div>
        <% } %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
